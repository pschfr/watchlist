nav.navbar.navbar-inverse.navbar-fixed-top
	.container
		.row
			.col-xs-12
				h1 #{title}
					span.pull-right
						a(href="#{githubURL}")
							if environment == "production"
								img(src="/watchlist/github.svg")
							else
								img(src="github.svg")
